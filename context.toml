[user]

role = "Experienced Software Engineer"

# Add your core technologies here, e.g., ["Python", "AWS", "Kubernetes"]

stack = [] 

[preferences]

# Choose one for your learning style:

# "Architect" (System design, diagrams)

# "Hacker" (Code, implementation details)

# "Reporter" (News, recent releases, "what's changed")

mode = "Hacker" 

# Model selection (gemini-2.0-flash-lite, gemini-1.5-flash, gemini-1.5-pro, etc.)
model = "gemini-2.5-pro"

# Topics the AI should never suggest

exclude_topics = ["Rust", "Basic HTML/CSS"]

